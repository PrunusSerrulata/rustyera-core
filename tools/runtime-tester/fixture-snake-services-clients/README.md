# Batch 1D real-client service slice

Prepared source, not executed. New-game seed 123456, fixed clock 2026-01-01T00:00:00Z.
Use isolated copies, no persistent Data from other runs. Submit 1 at START; then click the visible SNAKE_POINTER_TARGET button (script value 41); wait for READY. Canonical traces must capture service requests/responses and all three projection revisions. Five-second full DOM/runtime watchdog remains mandatory.

Expected semantic marker: SNAKE_HTML=0/0/0/1/1/1/1. These assertions cover empty-line lazy width, flag count, first-row length, one width evaluation per split, and source-preserving Unicode reconstruction (without a literal ampersand). Metrics are actual font-dependent observations, not cross-platform pixel promises.

Split budgets use the actual `HTML_STRINGLEN` half-width units. A literal one-unit budget can be narrower than an `A` in a platform fallback font and correctly fail with `NoProgress`; the dedicated no-progress fixture keeps that rejection separate. The width callback still changes its budget and increments its side-effect counter once per split.

Canvas 741 is never mounted: sample opaque red then blue immediately after writes. Expected SNAKE_CANVAS=4294901760/4278190335/2/2 for independent revision replay and two explicit 2px sprite widths. Default image height follows the configured font size and is covered separately; it is not the source bitmap height. The declared PNG is two authored pixels (2x1 red/half-alpha blue). Missing image measurement is observed but never conflated with a declared-file decode/hash failure.

Pointer is sampled after actual visible button input. `CLEARLINE 1` removes the accepted input row before sampling so the target remains under the real pointer. Assert the String MOUSEB="41", logical x in viewport and y relative to lower client edge. Hover remains observable while the input button is retired; it does not grant permission to submit it again. Separate driver actions must cover scroll/resize/leave/blur and stale async replies; this script alone is not evidence for those cases.

No SQL, title playability, new tags, Float or scene claims. TUI must refuse HTML v2; use the data-only combination fixture for its successful ingestion/structured/GLOBAL path. Distinct minimal unsupported fixtures are below the ignored staging area and will be promoted with tests.

R1 pixel replacement slice: an opaque blue pixel receives alpha128 pure red (2164195328), then
transparent clear0. Its opaque green neighbor must remain4278255360 after both writes. The shared
Browser/Tauri assertion requires `SNAKE_CANVAS_REPLACE=2164195328/4278255360/0/4278255360` from actual
GGETCOLOR on the unmounted canvas. No general guarantee is made for low-alpha RGB rounding. The
ordinary service oracle canvas case records corresponding same-input writes for both fixed engines.
