# Independent lifecycle successor

A distinct project identity/code and script used only after the old project has a real
`HTMLImageElement.decode()` awaiting the bounded PNG stream. Its fresh pure-color canvas request
must finish before the old bytes are released. Its separately owned lifecycle-next.png retains only the same tiny source fixture bytes;
its identity and loader generation belong to the new project. This is not a restart of the first fixture. See the lifecycle fixture README/driver.
